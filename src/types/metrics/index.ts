/**
 * What: Placeholder contract for metrics definitions.
 * Why: Keeps extension point ready for observability metadata.
 * How to use:
 * `interface RuleMetric extends IMetric { name: string }`
 */
export interface IMetric {}
