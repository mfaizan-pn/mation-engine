/**
 * What: Placeholder contract for trigger definitions.
 * Why: Future automation workflows will attach trigger typing here.
 * How to use:
 * `interface KafkaTrigger extends ITrigger { topic: string }`
 */
export interface ITrigger {}
