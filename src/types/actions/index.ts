/**
 * What: Placeholder contract for action definitions.
 * Why: Reserved for workflow/task action typing in future phases.
 * How to use:
 * `interface MyAction extends IAction { type: "HTTP_REQUEST" }`
 */
export interface IAction {}
