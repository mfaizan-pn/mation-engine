/**
 * What: Barrel export for HTTP API server helpers.
 * Why: Lets consumers embed/start the API server programmatically.
 * How to use:
 * `import { createRuleApiServer } from "mation-engines";`
 */
export * from "./server";
